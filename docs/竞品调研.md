# 竞品调研：开源同类产品与 Hermes Watch 能力对齐

> 2026-09 初研 · 结论驱动本版本（v0.3）的实现清单：**补齐「告警生命周期层」**
> （自动恢复 / 防抖 / 滞后 / 重发 / 免打扰 / 多渠道通知 / 离线检测）。

## 零、2026-09-12 复研：开源赛道现状与我们的真实位置

### 数据快照（shields.io / GitHub，2026-09-12）

| 产品 | Stars | 活跃度 | 最近 Release | 动向 |
|---|---|---|---|---|
| uptime-kuma | 91k | 提交到昨天 | 昨天（2.5.4） | 2.x 已是稳定线；仍在新增监控类型（SFTP）与通知 provider |
| netdata | 80k | 提交到今天 | 8 月 | 边缘全栈路线照旧 |
| glances | 34k | 提交到昨天 | 8 月 | — |
| signoz | 32k | 提交到昨天 | 上周三 | 重型可观测平台，弱相关 |
| beszel | 25k | 提交到上周四 | 9-03（v0.19.0） | ZFS 池/数据集监控、容器健康告警（通知附日志摘录）、systemd 告警、CPU iowait/steal；agent 强制 HTTPS 证书校验 |
| openobserve | 22k | 提交到昨天 | 昨天 | 重型可观测平台，弱相关 |
| keep | 12k | 提交到昨天 | 上周三 | 「AIOps 2.0」：YAML workflow 可触发动作（如重启服务器）——执行是规则驱动，非 agent 自主 |
| gatus | 12k | 提交到上周二 | 5 月（v5.36.0） | 成熟度打磨：PG 索引 15x 提速、OIDC 登录页品牌化、ClickUp 通知 |
| healthchecks | 10k | 提交到上周四 | 8 月 | 心跳拨测窄赛道 |
| k8sgpt | 8.2k | 提交到上周一 | 9 月 | K8s 诊断（只读） |
| kagent | 3.7k | 提交到昨天 | 上周二 | **CNCF 项目**：K8s 原生 AI agent 框架，自带 K8s/Istio/Helm/Prometheus 等 MCP 工具服务器——「agent + MCP 执行」路线的官方背书 |
| dashdot | 3.5k | 3 月后停更 | 3 月 | 休眠 |
| holmesgpt | 3.3k | 提交到上周三 | 上周二 | 企业 K8s 诊断，只读 |
| aurora（Arvo-AI/aurora） | 411 | 提交到 4 小时前 | 无 tag（589 commits，34 PR） | LangGraph 事件管理+RCA：吃 PagerDuty/Datadog 等现成告警，不自采集；修复走「生成 PR」而非自主执行；架构重（Flask+Celery+Memgraph+Weaviate+Vault） |
| **hermes-watch** | **1** | 提交到今天 | **无 release** | 本项目 |

### 三个结构性判断

1. **功能位没有对手，社区位是零。** 论功能：告警生命周期（初研 16 项缺失）已全部落地，
   覆盖面 = Beszel 的告警层 + Gatus 的拨测层，再叠加这三家都没有的 AI 深挖/根因卡/
   提案审批执行/MCP 只读工具面/堡垒机/多用户 RBAC。但在开源项目的标尺上——
   **1 star vs 91k，无 release，无贡献者**——我们目前是「个人完成的精品工程」，
   还不是「开源项目」。开源项目的分水岭不是功能清单，是别人的部署量与 issue 流。
2. **赛道空位仍在，且比初研时更清晰。** 关键词「monitoring + LLM agent diagnosis」
   搜索结果全是 0–1 star 的玩具仓。大玩家不往下做（Keep/Aurora/kagent 全部瞄准
   企业云/K8s，吃现成监控数据）；轻量玩家不做 AI 深度（Beszel/Gatus 的 release
   全是采集面打磨）；AI 玩家不做采集与轻量部署（Aurora 一个栈要 Memgraph+Weaviate+Vault）。
   「**把诊断-审批-执行闭环装进一台 2GB 内存自托管盒子里**」这个组合依然无人占据。
3. **MCP 与执行审批两条铁律都被赛道验证了。** glances 内置 MCP server、kagent 以
   MCP 为工具层、k8sgpt/holmesgpt 只读诊断——与我们的只读提议制方向一致；
   Aurora 的「修复生成 PR」和 Keep 的「YAML 规则动作」是另外两种执行闸门形态，
   都没有走 agent 自主执行，再次印证审批闸门是行业共识而非我们的保守。

### 对排队清单的竞争性建议

- **最缺的不是技术项，是开源基建**：打 tag 发 release、Docker 一键（Dockerfile/compose.yml
  已就绪）、英文 README、界面截图/GIF、（可选）在线 demo。这一步的成本远低于任何
  一个排队技术项，却是「在开源项目中怎么样」从 1 star 起步的唯一路径。
- 技术排队项按竞争价值排序：**拨测矩阵补宽（ICMP/DoH/SNMP）** > **metrics 降采样**
  （撑长期数据故事）> 二级跳板链（深运维场景，受众窄）。
- 可抄清单新增：Beszel 的 agent 证书校验（TOFU 我们已有，可对齐 HTTPS 校验强度）、
  Gatus 的 PG 性能思路不适用（我们是 SQLite 单机，本来就没这问题）。


## 一句话结论

Hermes Watch 在「诊断深度」（根因卡 + 证据链 + 提案审批执行）上领先全部轻量竞品；
真正成片缺失的是 Uptime Kuma / Gatus 已标准化的**告警生命周期层**——这一层全是
小体量可实现的能力，且不与我们的差异化定位冲突。补齐后可用
「**Gatus 的通知层 + holmesgpt 式诊断深度**」讲故事。

## 一、必查产品速览

| 产品 | Stars | 定位 | 与我们的关系 |
|---|---|---|---|
| [beszel](https://github.com/henrygd/beszel) | 25.2k | 轻量服务器监控（Hub+出站 Agent） | 架构同构；其 Shoutrrr 单 URL 多渠道、systemd 告警、每日免打扰值得抄 |
| [uptime-kuma](https://github.com/louislam/uptime-kuma) | 91.1k | 自托管拨测 | **通知/心跳教科书**：108 个通知 provider、PENDING 心跳防抖、恢复通知带宕机时长 |
| [netdata](https://github.com/netdata/netdata) | 80.5k | 边缘全栈可观测 | CLEAR/WARNING/CRITICAL 状态机 + 滞后阈值（trigger 80 / clear 50） |
| [gatus](https://github.com/TwiN/gatus) | 12k | 状态页+拨测 | **success-threshold 恢复模型**（连续 2 次成功 → resolved）+ send-on-resolved |
| [glances](https://github.com/nicolargo/glances) | 33.5k | top 替代 | 4.5.1 起内置 MCP server——「监控数据给 AI」已是通用趋势，我们方向正确 |
| [keep](https://github.com/keephq/keep) | 12.3k | AIOps 告警管理 | 告警去重/关联/富化参考；109 集成是大体量赛道，不追 |
| k8sgpt / holmesgpt | — | AI 诊断 | 只诊断不执行（holmesgpt 默认只读+RBAC，修复走显式工具通道）——与我们铁律完全一致；k8sgpt 的 `--anonymize` 脱敏值得抄 |
| dashdot / statping-ng | 3.5k / 2k | 展示型面板 | 无诊断无告警，反证我们「巡检=采集+诊断+报告」差异化成立；statping 半弃维护 |

## 二、核心事实（带证据）

- **Uptime Kuma 心跳模型**：`DOWN=0/UP=1/PENDING=2/MAINTENANCE=3`；失败先降级 PENDING
  （maxretries 次），耗尽才 DOWN；只有状态翻转的 "important" 心跳才发通知（天然去重）；
  恢复消息带 `lastDownTime`；持续 DOWN 按 `resendInterval` 周期重发。
  README 宣称 90+ 通知渠道，源码实数 108 个 provider。
- **Netdata 状态机**：CLEAR / WARNING / CRITICAL / UNDEFINED / UNINITIALIZED / REMOVED；
  官方滞后示例：`Triggers at 80, clears at 50, preventing flapping between 50-80`。
- **Gatus 恢复模型**：`failure-threshold`（默认 3）连续失败触发；
  `success-threshold`（默认 2）连续成功 → "marked as resolved"；`send-on-resolved` 开关。
- **Beszel 通知**：Shoutrrr URL schema 单表达式 23 渠道；v0.18-0.19 新增
  systemd 失败服务告警、容器健康告警（通知附日志摘录）、每日免打扰时段（quiet hours）。

## 三、缺失清单与本次落地情况

| # | 缺失能力 | 来源竞品 | 本次落地 |
|---|---|---|---|
| 1 | 告警状态机 + 自动恢复（resolved）+ 恢复通知 | Gatus / Netdata / UK | ✅ `scheduler._check_recovery`：连续 3 轮未再触发 → 自动恢复，事件 + 通知 |
| 2 | 恢复通知带持续时长 | UK `lastDownTime` | ✅ 恢复文案「持续 X 小时 Y 分」 |
| 3 | 连续 N 次防抖（PENDING 层） | Gatus `success-threshold` / UK maxretries | ✅ RECOVER_ROUNDS=3，抖动不打翻告警 |
| 4 | 阈值滞后（触发/恢复双阈值） | Netdata CLEAR 模式 | ✅ `rules.CLEAR_THRESHOLDS`：warn 85 触发 / 80 以下才算解除 |
| 5 | 多通知渠道 | UK 108 渠道 / Beszel Shoutrrr | ✅ 6 渠道（企业微信/钉钉/飞书/Telegram/Server酱/通用），设置页可选 + 测试按钮 |
| 6 | 免打扰时段 | UK 维护窗口 / Beszel quiet hours | ✅ `quiet_hours`（支持跨午夜），只记事件不外呼 |
| 7 | 持续告警周期重发 | UK `resendInterval` | ✅ `notify_resend_min`：crit 持续未恢复按周期提醒 |
| 8 | 主机离线检测 | UK 心跳 / Beszel status | ✅ `last_ok_ts` + 3×巡检周期无采集 → 离线；Fleet 徽标 + 拓扑灰色节点 |
| 9 | systemd 失败服务告警 | Beszel v0.19 | ✅（既有能力，本次证实为竞品验证过的方向） |
| 10 | 通知发送留痕 | UK 通知历史 | ✅ `notify_log` 表：成功/失败/免打扰拦截全留痕，设置页展示最近记录 |
| 11 | LLM 出站脱敏（anonymize） | k8sgpt `--anonymize` | ✅ `ai_anonymize` 开关：主机名/IP/用户名出站前替换占位符，单趟正则防二次脱敏，回答映射回真实名 |
| 12 | 心跳历史条带图（24h 上/下） | UK ping chart | ✅ 主机详情 48 桶巡检心跳带（随时间窗缩放），指标阈值映射 ok/warn/crit |
| 13 | PWA（manifest + SW） | UK / statping-ng | ✅ manifest + service worker（仅生产注册，SW 纯透传不缓存——巡检数据必须强实时） |
| 14 | AI 对话流式输出 | 主流 AI 产品标配 | ✅ `/api/chat/stream` SSE 逐 token，LLM 关闭降级本地摘要（已修复空 API Key 时 Bearer 头导致的 LocalProtocolError） |
| 15 | 公开状态页 / 只读分享 | UK / Gatus | ✅ `/status/{token}` 只读页：健康概览 + 主机状态 + 发现标题，60s 自刷新，可撤销/换新，不含地址与证据 |
| 16 | i18n 骨架 | UK Weblate / Beszel 中文站 | ✅ 轻量自建（零依赖）：`i18n.ts` 翻译表 + `useT()` hook + PageHead 语言切换，默认 zh、en 回落 zh；语言偏好存 localStorage 并同步后端 settings；骨架覆盖导航/页头/状态词/通用按钮，未迁移 key 自动回落中文 |

**刻意不追的**：Netdata 边缘 ML、holmesgpt 100+ toolset、Keep 109 集成、
Portainer/Komodo 部署管理面（定位不同）、statping-ng（半弃维护）。

## 四、本次顺带修复的正确性问题（真实使用会露馅的）

1. **真实主机根因卡不再展示演示编造文案**——`analysis.py` 区分 mock/真实：
   真实主机根因只陈述规则事实，提案命令里的 IP/服务名来自该机真实证据
   （此前会把演示 IP `185.220.101.34` 写进真实提案）。
2. **密钥登录的主机可以执行提案了**——`executor._real_exec` 与采集侧统一走
   本机默认密钥（此前空密码时传空密钥列表，免密主机执行必失败）。
3. **agent push 防 NaN/Inf**——一个坏点不再能污染全部阈值判断与图表。
4. **会话可撤销**——改口令/开关访问控制递增 session_epoch，旧 Cookie 全部立即失效。
5. **删除主机级联清理** findings/proposals/events/proposal_runs。
6. **启动不再被慢主机阻塞**——首轮巡检后台执行；采集并发化（信号量限流 6）。
7. **CORS 全开中间件移除**（前端走 vite 代理本就同源）。
